1. Create a AI Hub to allow switching between Gemini and OpenRouter. If GEMINI_API_KEY is not provided, use OpenRouter by default. If both GEMINI_API_KEY and OPENROUTER_API_KEY are not provided, flag this as an error

2. When you open and close order, populate the trade journal as well